// Root layout obrigatório para o Next.js.
// O layout real fica em [locale]/layout.tsx (padrão next-intl).
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
